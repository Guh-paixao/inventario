services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=inventario_db'
      - 'POSTGRES_PASSWORD=123456'
      - 'POSTGRES_USER=admin'
    ports:
      - '5432:5432'
